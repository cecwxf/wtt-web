export const SOURCE_FLOW_HEADER = '┌─ 来源标识 ─────────────'
export const SOURCE_FLOW_FOOTER = '└────────────────────'

export function buildWttUserSourceFlow(_senderName: string, content: string): string {
  return content
}
